<resources>

    <style name="AppTheme" parent="AppTheme.Base">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.Archive" parent="AppTheme.Base">
        <item name="colorPrimary">@color/colorArchive</item>
        <item name="colorPrimaryDark">@color/colorArchiveDark</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="colorControlHighlight">@color/colorHighlight</item>

        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@color/colorDivider</item>
        <!--<item name="android:actionModeCloseButtonStyle">@color/colorPrimaryDark</item>-->
        <item name="actionBarPopupTheme">@style/Theme.AppCompat.Light</item>
    </style>

    <style name="toolbar_text">
        <item name="android:textColor">#000000</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">8</item>
        <item name="android:shadowDy">8</item>
        <item name="android:shadowRadius">4</item>
    </style>

    <style name="SaveUpdateButtonStyle">
        <item name="android:background">@drawable/save_update_button_background</item>
        <item name="android:textColor">@drawable/save_update_button_text_color</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="ActionBarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Light" >
        <item name="android:background">@android:color/white</item>
        <item name="android:textColor">#000</item>
    </style>

</resources>
